<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat&family=Roboto:wght@300&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/global.css" />
    <link rel="stylesheet" href="/css/listaProductos.css" />

    <title>Inventario de productos</title>
  </head>
  <body style="align-self: center">
    <%- include('../partials/header') %>
    <div class="listaProductos-wrapper">
      <h2>Editar Producto</h2>
      <section>
        <div class="formulario-creacion">
          <form method="POST" action="/listaProductos/<%=productoAEditar.id%>/editar?_method=PUT">
            <div class="campos-formulario">
              <div>
                <label for="id">ID</label>
                <input
                  type="number"
                  name="id"
                  readonly
                  value="<%=productoAEditar.id%>"
                  id="id"
                />
                <label for="descripcion">Descripción</label>
                <textarea name="descripcion" id="descripcion">
<%=productoAEditar.descripcion%>
              </textarea
                >
              </div>
              <div>
                <label for="nombre">Nombre</label>
                <input
                  type="text"
                  name="nombre"
                  id="nombre"
                  value="<%=productoAEditar.nombre%>"
                />

                <label for="tipo">Tipo de producto</label>
                <select name="tipo" id="tipo">
                  <option value="alimento" <%= productoAEditar.tipo == 'alimento' ? 'selected' : ''%>>alimento</option>
                  <option value="juguete" <%= productoAEditar.tipo == 'juguete' ? 'selected' : ''%>>juguete</option>
                </select>
                <input
                  class="crear-boton no-mobile"
                  type="submit"
                  value="Guardar Cambios"
                />
              </div>
              <div>
                <label for="precio">Precio</label>
                <input
                  type="number"
                  name="precio"
                  id="precio"
                  value="<%=productoAEditar.precio%>"
                />
                <label for="imagen">Archivo imagen</label>
                <select name="imagen" id="imagen">
                  <% for(let opcion in archivosImagen){ %>
                  <option value="<%=archivosImagen[opcion]%>" <%= productoAEditar.imagen == archivosImagen[opcion] ? 'selected' : ''%>>
                    <%=archivosImagen[opcion]%>
                  </option>
                  <%} %>
                </select>
                <input
                  class="no-desktop crear-boton"
                  type="submit"
                  value="Guardar Cambios"
                />
                <button class="no-mobile">Limpiar Campos</button>
              </div>
            </div>
          </form>
        </div>
      </section>
    </div>
    <%- include('../partials/footer') %>
  </body>
</html>
